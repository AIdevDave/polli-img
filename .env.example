# Pollinations.ai API Configuration
# Get your API key from https://enter.pollinations.ai

# Your Pollinations.ai API Key
# Use secret keys (sk_) for server-side applications with no rate limits
# Use publishable keys (pk_) for client-side applications (IP rate-limited)
POLLINATIONS_API_KEY=your_api_key_here
